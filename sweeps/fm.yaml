program: src/train.py
entity: benhayes
project: synth-permutations
command:
  - python
  - src/train.py
  - ${args_no_hyphens}
name: fm_sweep
method: grid
metric:
  goal: minimize
  name: val/lsd
parameters:
  experiment:
    values:
      - fm/flow_asym
      - fm/ffn_mse
      - fm/ffn_asym
      - fm/flow
  "experiment/fm/algorithm":
    values:
      - conditional
      - mixed
      - hierarchical
