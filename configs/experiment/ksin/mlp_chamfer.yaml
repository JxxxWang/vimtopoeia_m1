# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: ksin
  - override /model: ksin_ff
  - override /callbacks: default
  - override /trainer: gpu

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

experiment_name: ksin

tags:
  - ksin
  - mlp
  - chamfer
  - k_${data.k}

seed: 12345

model:
  loss_fn: chamfer

trainer:
  min_epochs: 10
  max_epochs: 1000
  limit_val_batches: 0.0

logger:
  wandb:
    tags: ${tags}
    group: ${data.k}sin
    name: ${data.k}sin_mlp_chamfer

callbacks:
  model_checkpoint:
    monitor: train/loss
