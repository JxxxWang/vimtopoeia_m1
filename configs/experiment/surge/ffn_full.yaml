# @package _global_

defaults:
  - surge/base
  - override /model: surge_ffn_full
  - override /data: surge
  - _self_

run_name: ffn
experiment_name: surge-full

tags:
  - surge

callbacks:
  log_per_param_mse:
    _target_: src.utils.callbacks.LogPerParamMSE
    param_spec: surge_xt
  plot_projii: null
