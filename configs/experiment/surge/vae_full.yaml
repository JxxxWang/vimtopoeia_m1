# @package _global_

defaults:
  - surge/base
  - override /model: surge_flowvae
  - _self_

model:
  net:
    latent_dim: 300

experiment_name: surge-full-onehot
run_name: vae

callbacks:
  plot_proj_ii: null
